//if javascript is enabled, this document will load and thus it re-enables the toggler-icon on small pages. Otherwise this element is hidden since javascript controls the collapse/expand function of the button anyway.

document.getElementById("navbar-toggler-icon-id").style.display = "inline-block";